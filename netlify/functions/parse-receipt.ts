import type { Handler } from '@netlify/functions';import { callOpenAI,json } from './_shared/openai';
export const handler:Handler=async(event)=>{if(event.httpMethod==='OPTIONS')return json(200,{});try{const {imageBase64,mediaType='image/jpeg',store='Otro'}=JSON.parse(event.body||'{}');if(!imageBase64)return json(400,{error:'Falta la imagen'});const prompt=`Lee este ticket de ${store}. Devuelve exclusivamente JSON válido: {"items":[{"name":"","category":"Frutas y verduras|Carnes y pescado|Lácteos y huevos|Despensa|Limpieza y hogar|Otros","quantity":1,"unit_price":null,"total_price":0}],"total":0}. Extrae cada producto. quantity puede ser null. unit_price puede ser null si no se lee. Ignora subtotal, envío, propina, IVA, descuentos aislados y formas de pago, pero reporta el total general. Usa exactamente una de las categorías permitidas.`;const data=await callOpenAI([{type:'text',text:prompt},{type:'image',source:{type:'base64',media_type:mediaType,data:imageBase64}}],{task:'receipt_ocr',maxOutputTokens:2500});return json(200,data)}catch(e){return json(500,{error:e instanceof Error?e.message:'Error inesperado'})}};
