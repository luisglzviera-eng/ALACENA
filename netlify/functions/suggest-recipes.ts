import type { Handler } from '@netlify/functions';import { callOpenAI,json } from './_shared/openai';
export const handler:Handler=async(event)=>{if(event.httpMethod==='OPTIONS')return json(200,{});try{const body=JSON.parse(event.body||'{}');const prompt=`Eres el asistente culinario de una pareja en México. Responde únicamente JSON válido con esta forma: {"suggestions":[{"name":"...","why":"..."}]}. Genera ${body.mealType==='snack'?'5 a 8':'4 a 6'} ideas de tipo ${body.mealType||'comida normal'}. Respeta ambas dietas. Usa ingredientes comunes de HEB, Calimax o Calii en México. ${body.preferences?.budget_priority?'Prioriza ingredientes económicos, rendidores y de temporada; evita cortes caros y productos gourmet.':''} ${body.mealType==='para niños'?'Deben requerir máximo 20-25 minutos activos, sabores sencillos y poco condimento.':''} Prioriza recetas guardadas y evita repetir comidas recientes. Dieta persona 1: ${body.preferences?.diet_user||'sin datos'}. Dieta persona 2: ${body.preferences?.diet_partner||'sin datos'}. Recetas guardadas: ${JSON.stringify(body.recipes||[])}. Comidas recientes: ${JSON.stringify(body.recentMeals||[])}`;const data=await callOpenAI([{type:'text',text:prompt}],{task:'recipe_suggestions',maxOutputTokens:1500});return json(200,data)}catch(e){return json(500,{error:e instanceof Error?e.message:'Error inesperado'})}};
