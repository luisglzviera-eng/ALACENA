import type { Handler } from '@netlify/functions';import { callOpenAI,json } from './_shared/openai';
export const handler:Handler=async(event)=>{if(event.httpMethod==='OPTIONS')return json(200,{});try{const {imageBase64,mediaType='image/jpeg',person='persona'}=JSON.parse(event.body||'{}');if(!imageBase64)return json(400,{error:'Falta la imagen'});const prompt=`Analiza el plan de alimentación de ${person}. Devuelve exclusivamente JSON válido: {"summary":"..."}. Resume en español de México y máximo 60 palabras qué priorizar, qué evitar, porciones o restricciones claramente visibles. No diagnostiques ni inventes datos ilegibles.`;const data=await callOpenAI([{type:'text',text:prompt},{type:'image',source:{type:'base64',media_type:mediaType,data:imageBase64}}],{task:'diet_analysis',maxOutputTokens:700});return json(200,data)}catch(e){return json(500,{error:e instanceof Error?e.message:'Error inesperado'})}};
